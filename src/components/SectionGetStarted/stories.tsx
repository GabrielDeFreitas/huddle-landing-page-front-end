import { Story, Meta } from '@storybook/react'
import SectionGetStarted from '.'

export default {
  title: 'SectionGetStarted',
  component: SectionGetStarted
} as Meta

export const Default: Story = () => <SectionGetStarted />
